int adjacentElementsProduct(int[] inputArray) {
    int max=inputArray[0]*inputArray[1];
    for(int i=1; i<inputArray.length-1; i++){
        int produce=inputArray[i]*inputArray[i+1];
        if(produce>max){
            max=produce;
        }
    }
    return max;
}
